"""STRATA core: provenance primitives, boundary contracts, and config.

This package is the cloud-agnostic, pure foundation. Nothing here may import a
source client, an index backend, or a deploy concern — dependencies point *into*
core, never out of it.
"""
